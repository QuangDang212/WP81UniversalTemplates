<base:PageBase
    x:Class="WP81Template.Views.MainPage"
    xmlns:base="using:WP81Template.Views.Base"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WP81Template.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="24,12">
            <TextBlock Text="Template" Style="{ThemeResource TitleTextBlockStyle}"
                        TextWrapping="Wrap"/>
            <TextBlock Text="WP 8.1" Style="{ThemeResource HeaderTextBlockStyle}"
                        TextWrapping="Wrap"/>
        </StackPanel>

    </Grid>
</base:PageBase>
